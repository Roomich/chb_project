pipeline {
    agent { label "jenkins-agent" }
    // agent any

    tools {
        terraform 'terraform'
    }

    options {
        buildDiscarder(logRotator(numToKeepStr: '10'))
        disableConcurrentBuilds()
        timestamps()
        ansiColor('xtrem')
    }

    environment {
        // AWS_ACCESS_KEY_ID = credentials('accessKey')
        AWS_ACCESS_KEY_ID = 'AKIAU4L6PJKVHTU3DQGZ'
        // ASW_SECRET_ACCESS_KEY = credentials('secretKey')
        ASW_SECRET_ACCESS_KEY = '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'
        AWS_DEFAULT_REGION = 'us-east-1'
        PROJECT_DIR = 'terraform/infrastructure'
    }

    stages {
        // stage ('Checkout SCM') {
        //     steps {
        //         checkout scmGit(branches: [[name: 'Buildapp']], extensions: [], userRemoteConfigs: [[credentialsId: 'github_ssh', url: 'git@github.com:Roomich/chb_project.git']])
        //     }
        // }

        stage('Terraform init') {
            steps {
                dir(PROJECT_DIR) {
                    sh "terraform init"
                }
            }
        }

        stage('Terraform validate') {
            steps {
                dir(PROJECT_DIR) {
                    sh "terraform validate"
                }
            }
        }
    }
}